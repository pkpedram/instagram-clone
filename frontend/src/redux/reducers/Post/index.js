let initialState = {
    
};

const postState = (state = initialState, action) => {
  let { type, payload, params } = action;

  switch (type) {
    default:
      return state;
  }
};

export default postState;
